<!doctype html>
<html lang="he" dir="rtl">
<head>
  <meta charset="utf-8">
  <title>{{ page.title | default: site.title }}</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="{{ '/assets/style.css' | relative_url }}">
  <!-- Leaflet למפות אינטראקטיביות בדפי עומק -->
  <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css"/>
  <script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script>
</head>
<body>
  <header>
    <h1><a href="{{ '/' | relative_url }}">וילנה וקובנה — נוב' 2025</a></h1>
    <nav class="tabs">
      <a href="{{ '/' | relative_url }}">לו"ז ומידע Live</a>
      <a href="{{ '/vilnius.html' | relative_url }}">וילנה</a>
      <a href="{{ '/kaunas.html' | relative_url }}">קובנה</a>
    </nav>
  </header>
  <main id="content">
    {{ content }}
  </main>
  <footer>
    <small>האתר נטען מקבצי _data ומתעדכן אוטומטית בפרסום ל‑GitHub Pages.</small>
  </footer>
</body>
</html>
